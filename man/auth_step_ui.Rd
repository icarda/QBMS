% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{auth_step_ui}
\alias{auth_step_ui}
\title{Authentication Step UI Dispatcher}
\usage{
auth_step_ui(engine, no_auth)
}
\arguments{
\item{engine}{Character string: one of "bms", "breedbase", "deltabreed", "ebs", "germinate", "gigwa".}

\item{no_auth}{Logical. If TRUE, no authentication UI is rendered.}
}
\value{
A \code{shiny::tagList} with the appropriate auth inputs, or NULL if
  no authentication is required.
}
\description{
Returns the appropriate authentication UI based on the engine and no_auth
setting. Uses \code{get_auth_step_type()} to determine whether credentials,
token, or no authentication UI should be rendered.
}
\examples{
if (interactive()) {
  auth_step_ui("bms", FALSE)
  # Returns credentials UI (username + password + submit)

  auth_step_ui("ebs", FALSE)
  # Returns token UI (token + submit)

  auth_step_ui("bms", TRUE)
  # Returns NULL
}

}
\keyword{internal}
